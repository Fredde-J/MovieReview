export default class Movie {
    constructor(title, rating, genre, text){
            this.title = title
            this.rating = rating
            this.genre = genre
            this.text = text
    }
}